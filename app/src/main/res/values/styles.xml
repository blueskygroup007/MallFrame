<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--带toolbar的activity的主题-->
    <style name="AppTheme.Toolbar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!--Splash的全屏白色主题-->
    <style name="SplashPageTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>


    <!--AS创建全屏Activity时创建的主题-->
    <style name="FullscreenTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">?android:attr/buttonBarStyle</item>
        <item name="metaButtonBarButtonStyle">?android:attr/buttonBarButtonStyle</item>
    </style>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>
    <!--AS创建全屏Activity时创建的主题-->

    <!--自定义Dialog的动画-->
    <style name="pop_anim_style">
        <item name="android:windowEnterAnimation">@anim/pop_in</item>
        <item name="android:windowExitAnimation">@anim/pop_out</item>

    </style>

    <!--自定义Dialog的主题-->
    <style name="dialog_custom" parent="android:Theme.Holo.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>

    </style>

    <!--底部标签栏按钮样式-->
    <style name="MaterialBottomNavBottonStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>

        <item name="android:textSize">
            @dimen/material_bottom_nav_bar_fixed_action_active_label_text_size
        </item>
        <!--这个选项不是将图片资源放在上方,而是listview的属性-->
        <!--<item name="android:drawSelectorOnTop">true</item>-->
        <!--颜色又是一个选择器-->
        <item name="android:textColor">@drawable/main_radio_button_selector</item>
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item>
    </style>

    <!--倒班设置中，标题文本的样式-->
    <style name="CaptionSolutionEdit">


    </style>

    <!--todo 知识点:学习这种style的设置方法.-->
    <!--在布局文件中,是没有editTextStyle这个属性的.但是在style中就可以写-->
    <style name="Theme.TimePicker" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:editTextStyle">@style/Widget.AppCompat.EditText.Gray</item>
        <item name="android:padding">0dp</item>
        <item name="android:layout_margin">0dp</item>
    </style>

    <style name="Widget.AppCompat.EditText.Gray">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <!--拷贝出来的TimePicker的style-->
    <style name="Widget.Material.TimePicker">
        <item name="timePickerMode">@integer/time_picker_mode_material</item>
        <item name="legacyLayout">@layout/time_picker_legacy_material</item>
        <!-- Attributes for new-style TimePicker. -->
        <item name="internalLayout">@layout/time_picker_material</item>
        <item name="headerTextColor">@color/primary_text_secondary_when_activated_material</item>
        <item name="headerBackground">#ff555555</item>
        <item name="numbersTextColor">?attr/textColorPrimaryActivated</item>
        <item name="numbersInnerTextColor">?attr/textColorSecondaryActivated</item>
        <item name="numbersSelectorColor">?attr/colorControlActivated</item>
        <item name="numbersBackgroundColor">#ff555555</item>
        <item name="amPmTextColor">?attr/textColorSecondary</item>
    </style>

</resources>
